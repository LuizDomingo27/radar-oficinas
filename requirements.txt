# App unificado: Radar de Oficinas (pipeline + SPA) + Gestão de Postos (Supabase).
#
# TRAVA TOTAL DE VERSÕES (pin exato de tudo, inclusive transitivas).
# Motivo: o Streamlit Cloud reconstrói o ambiente do zero de tempos em tempos e,
# com faixas (>=), instalaria a versão MAIS NOVA disponível — um lançamento novo
# de qualquer lib (ou transitiva) pode quebrar o app sem mudança de código nossa.
# Com tudo em "==", a instalação no Cloud fica reprodutível e imune a isso.
# Casa com runtime.txt (Python 3.12). Conjunto validado: 264 testes passam
# (3 pulam só por pypdf, que é dependência de TESTE — não vai aqui).
#
# Para atualizar (deliberadamente, nunca por acaso): suba a versão desejada num
# venv 3.12 limpo, rode `pytest tests/`, congele com `pip freeze` e substitua
# este arquivo. Se um upgrade quebrar em produção, `git revert` volta ao conjunto
# bom em minutos.

# --- Dependências diretas ------------------------------------------------------
# Radar (pipeline + SPA embutida):
streamlit==1.58.0
openpyxl==3.1.5
requests==2.32.5

# Gestão de Postos (app_postos/): dashboard nativo com dados ao vivo no Supabase.
pandas==2.3.2
numpy==2.3.3
reportlab==4.5.0
supabase==2.31.0

# --- Transitivas travadas (resolvidas pelo pip; não editar à mão) --------------
altair==6.2.2
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.8
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cryptography==50.0.1
deprecation==2.1.0
et_xmlfile==2.0.0
gitdb==4.0.12
GitPython==3.1.62
h11==0.16.0
h2==4.4.1
hpack==4.2.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
hyperframe==6.1.0
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
multidict==6.8.0
narwhals==2.26.0
packaging==26.3
pillow==12.3.0
postgrest==2.31.0
propcache==0.5.2
protobuf==7.36.1
pyarrow==25.0.1
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
pydeck==0.9.3
PyJWT==2.14.0
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytz==2026.3.post1
realtime==2.31.0
referencing==0.37.0
rpds-py==2026.6.3
six==1.17.0
smmap==5.0.3
starlette==1.6.0
storage3==2.31.0
StrEnum==0.4.15
supabase-auth==2.31.0
supabase-functions==2.31.0
tenacity==9.1.4
toml==0.10.2
typing-inspection==0.4.4
typing_extensions==4.16.0
tzdata==2026.4
urllib3==2.7.0
uvicorn==0.53.0
watchdog==6.0.0
websockets==15.0.1
yarl==1.24.5
